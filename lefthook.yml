#
#   Refer for explanation to following link:
#   https://lefthook.dfoev/configuration/
#

min_version: 2.0.8

# Build commit messages
prepare-commit-msg:
  commands:
    commitzen:
      interactive: true
      run: npx cz --hook # Or npx cz --hook
      env:
        LEFTHOOK: 0

# Validate commit messages
commit-msg:
  commands:
    "lint commit message":
      run: npx commitlint --edit {1}

pre-commit:
  parallel: false
  commands:
    check:
      glob: "*.{js,ts,cjs,mjs,d.cts,d.mts,jsx,tsx,json,jsonc,ejs}"
      run: npx @biomejs/biome check --write --no-errors-on-unmatched --files-ignore-unknown=true --colors=off {staged_files}
      stage_fixed: true
      